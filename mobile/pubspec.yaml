name: sentence_reading
description: 문장 읽기 — Android Flutter client for A-sentence-reading Cloud Run API
publish_to: "none"
version: 0.2.86+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.3.0
  audioplayers: ^6.1.0
  google_sign_in: ^6.2.2
  flutter_web_auth_2: ^4.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
